const INITIAL_STATE: any = {
    data: [],
};

function userReducer(state = INITIAL_STATE, action: any): any {
    switch (action.type) {
        default:
            return state;
    }
}

export default userReducer;